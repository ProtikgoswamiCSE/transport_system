import 'package:flutter/material.dart';

class DrMapPage extends StatelessWidget {
  const DrMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("Google Map be displayed here"),
      ),
    );
  }
}
